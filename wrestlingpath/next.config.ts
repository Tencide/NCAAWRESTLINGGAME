import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // If hosting at GitHub Pages under a repo (e.g. username.github.io/cs319/),
  // uncomment and set to your repo name:
  // basePath: "/cs319",
  // assetPrefix: "/cs319/",
};

export default nextConfig;
